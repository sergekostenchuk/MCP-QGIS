# MCP-QGIS local profile (Mac + desktop QGIS)
MCP_PROFILE=local
MCP_HOST=127.0.0.1
MCP_PORT=8765
MCP_LOG_LEVEL=INFO
MCP_DATA_ROOT=./runtime
MCP_ENABLE_EXECUTE_CODE=false
# Optional path with algorithm ids, one per line
# MCP_ALLOWED_ALGORITHMS_FILE=./deploy/profiles/allowed_algorithms.txt
