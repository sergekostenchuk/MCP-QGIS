# Contributing

## Publish to GitHub

```bash
git remote add origin git@github.com:<your-user>/<your-repo>.git
git push -u origin main
```

## Pre-publish checklist

- Check `git status` is clean.
- Verify there are no secrets/private data in commits.
- Run tests:

```bash
pytest
```
